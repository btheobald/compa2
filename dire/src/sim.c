#include "sim.h"

// Distance
float getComponentDistance(body* bA, body* bB, int xy) {
  return 0;
}

float getVectorDistance(body* bA, body* bB) {
  return 0;
}

// Calculate Forces
int checkIfNeeded(uint8_t* forceMark, int bA, int bB) {
  return 0;
}

int markCalculation(uint8_t* forceMark, int bA, int bB, uint8_t status) {
  return 0;
}

float calculateAcceleration(body* bA, body* bB) {
  return 0;
}

void itteration(int cbc) {

}
